name: flutter_coverage_updater
description: Flutter coverage updater that generates coverage reports, HTML files, and SVG badges

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
repository: https://github.com/egortabula/egortabula_scripts/tree/main/flutter_coverage_updater

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 1.2.0

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.1

# Variables specify dynamic values that your brick depends on.
vars:
  generate_html_report:
    type: boolean
    description: Generate HTML coverage report
    default: true
    prompt: Do you want to generate HTML coverage report?
